name: Matrix Kernel Builder (Multi-Method)

on:
  workflow_dispatch:
    inputs:
      kernel_name:
        description: 'Nama Kernel (Contoh: Zixine-Elysium)'
        required: true
        default: 'Zixine'
        type: string

permissions:
  contents: write

jobs:
  build-kernel:
    name: Build ${{ matrix.ksu_variant }}
    runs-on: ubuntu-22.04
    
    strategy:
      fail-fast: false
      matrix:
        ksu_variant: [wild, sukisu, mambo]

    steps:
      - name: Extreme Disk Cleanup
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL /usr/local/share/boost
          sudo docker image prune --all --force
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*

      - name: Maximize Build Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 2048
          swap-size-mb: 8192 # WAJIB 8GB! LTO Thin di GKI butuh RAM besar saat linking Image.lz4
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          remove-docker-images: 'true'

      - name: Fetch Source Code
        uses: actions/checkout@v4

      - name: Initialize Git Identity
        run: |
          git config --global user.name "zixine"
          git config --global user.email "waheid005@gmail.com"

      - name: Dependency Installation
        run: |
          sudo apt-get update
          sudo apt-get install -y git curl repo build-essential bc bison flex libssl-dev \
            libelf-dev python3 python3-pip unzip zip rsync libncurses-dev lld
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*

      - name: Init & Sync GKI
        run: |
          mkdir gki && cd gki
          repo init -u https://android.googlesource.com/kernel/manifest -b common-android12-5.10 --depth=1 --groups=all,-notdefault,-device,-mips,-x86,-darwin
          repo sync -c --no-tags --no-clone-bundle --optimized-fetch -j$(nproc --all)
          
      - name: Update kernel source for GKI 5.10
        run: |
          cd gki/common
          git remote add rama https://github.com/ramabondanp/android_kernel_common-5.10.git
          git fetch -j12 rama
          git checkout -b update-android12-5.10-staging rama/android12-5.10-staging
          rm -rf drivers/staging/kernelsu

      - name: Apply Infinix GT 20 Pro Camera Fix
        working-directory: gki/common
        run: |
          curl -L "https://github.com/ramabondanp/android_kernel_common-5.10/commit/4fe04b60009e.patch" -o infinix_cam.patch
          patch -p1 < infinix_cam.patch || echo "✅ Patch kamera sudah tertanam."

      - name: Setup Variant (${{ matrix.ksu_variant }})
        working-directory: gki/common
        run: |
          VARIANT="${{ matrix.ksu_variant }}"
          clean_ksu() {
            rm -rf drivers/staging/kernelsu drivers/kernelsu KernelSU
            find drivers -name "Makefile" -exec sed -i '/kernelsu/d' {} + || true
            find drivers -name "Kconfig" -exec sed -i '/kernelsu/d' {} + || true
          }

          if [ "$VARIANT" = "sukisu" ]; then
            curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/main/kernel/setup.sh" | bash -s builtin
            git clone https://gitlab.com/simonpunk/susfs4ksu/ -b gki-android12-5.10 sus
            rm -rf sus/.git
            susfs=sus/kernel_patches
            cp -r $susfs/fs .
            cp -r $susfs/include .
            cp -r $susfs/50_add_susfs_in_gki-android12-5.10.patch .
          patch -p1 < 50_add_susfs_in_gki-android12-5.10.patch
          echo "[✓] SUSFS patched"
          elif [ "$VARIANT" = "wild" ]; then
            clean_ksu
            curl -LSs "https://raw.githubusercontent.com/WildKernels/Wild_KSU/wild/kernel/setup.sh" | bash -s wild
            git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android12-5.10 susfs-git
            cp -r susfs-git/kernel_patches/include/* include/
            cp -r susfs-git/kernel_patches/fs/* fs/
            patch -p1 < susfs-git/kernel_patches/50_add_susfs_in_gki-android12-5.10.patch || true
          elif [ "$VARIANT" = "mambo" ]; then 
            clean_ksu
            curl -LSs "https://raw.githubusercontent.com/Kingfinik98/KernelSU/refs/heads/main/kernel/setup.sh" | bash -s main
            curl -LSs "https://raw.githubusercontent.com/Kingfinik98/gki-builder/15ddb8c469eab27491d5bb0f8f66b7fe2c15d0d7/kernel-patches/ksu/rksu-add-mambosu-manager-support.patch" -o /tmp/rksu_manager.patch
            cd KernelSU && patch -p1 < /tmp/rksu_manager.patch && cd ..
            [ -f "drivers/kernelsu/Kbuild" ] && sed -i 's/$(error KSU requires Kprobes; enable it (defconfig))/# $(error KSU requires Kprobes; enable it (defconfig))/' drivers/kernelsu/Kbuild
          fi
          
      - name: Apply Critical Fixes
        working-directory: gki/common
        run: |
          echo "🔧 Patching Kconfig for Stack Protector Control..."
          # Fix Regex agar tidak salah sasaran di arch/arm64/Kconfig
          sed -i '/config STACKPROTECTOR_PER_TASK/,/def_bool y/ s/def_bool y/bool "Stack Protector per task"\n\tdefault n/' arch/arm64/Kconfig
          
          echo "🔧 Fixing Xiaomi Module Version mismatch..."
          sed -i '/pr_warn.*disagrees about version of symbol.*/,+1 s/.*/return 1;/' kernel/module.c
          
          echo "🔧 Removing DRM Helper Panics (Common GKI Bootloop)..."
          sed -i '/^static int drm_atomic_check_valid_clones/,/^}/d' drivers/gpu/drm/drm_atomic_helper.c
          sed -i '/ret = drm_atomic_check_valid_clones/,/return ret;/d' drivers/gpu/drm/drm_atomic_helper.c
          
          sed -i 's/check_defconfig//' build.config.gki

      - name: Replace 300Hz with 500Hz
        working-directory: gki/common
        run: |
          sed -i 's/config HZ_300/config HZ_500/g; s/bool "300 HZ"/bool "500 HZ"/g; s/default 300 if HZ_300/default 500 if HZ_500/g' kernel/Kconfig.hz

      - name: Clean Build Config Line
        run: sed -i '2d' gki/common/build.config.gki
        
      - name: Configure Kernel
        working-directory: gki/common
        run: |
          TARGET_CONFIG="arch/arm64/configs/gki_defconfig"
          VARIANT="${{ matrix.ksu_variant }}"
          
          # SANITASI: Hapus semua config lama biar tidak bentrok
          sed -i '/CONFIG_STACKPROTECTOR_PER_TASK/d' $TARGET_CONFIG
          sed -i '/CONFIG_HZ_/d' $TARGET_CONFIG
          sed -i '/CONFIG_TCP_CONG_BBR/d' $TARGET_CONFIG
          sed -i '/CONFIG_KSU/d' $TARGET_CONFIG
          sed -i '/CONFIG_BPF_JIT/d' $TARGET_CONFIG
          sed -i '/CONFIG_CPU_FREQ_GOV_ONDEMAND/d' $TARGET_CONFIG
          
          echo "⚙️ Injecting Configuration..."
          cat >> $TARGET_CONFIG <<EOF
          # Anti-Random Reboot Guard
          CONFIG_STACKPROTECTOR_PER_TASK=n
          CONFIG_CFI_CLANG=n
          
          # Performance
          CONFIG_HZ_500=y
          CONFIG_TCP_CONG_BBR=y
          CONFIG_DEFAULT_BBR=y
          CONFIG_BPF_SYSCALL=y
          CONFIG_BPF_JIT=y
          CONFIG_BPF_JIT_ALWAYS_ON=y
          CONFIG_BPF_JIT_DEFAULT_ON=y
          
          # Base GKI Configs (Do not remove)
          CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
          CONFIG_CPU_FREQ_GOV_ONDEMAND=y
          CONFIG_ENERGY_MODEL=y
          CONFIG_PM_AUTOSLEEP=y
          CONFIG_PM_WAKELOCKS=y
          CONFIG_LOCALVERSION_AUTO=n
          CONFIG_PSI=y
          CONFIG_MEMCG=y
          CONFIG_ANDROID_LOW_MEMORY_KILLER=y
          EOF

          if [ "$VARIANT" != "mambo" ]; then
            cat >> $TARGET_CONFIG <<EOF
          CONFIG_KSU=y
          CONFIG_KPM=y
          CONFIG_KSU_SUSFS_SUS_SU=n
          CONFIG_KSU_SYSCALL_HOOK=n
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT_SPOOF_GENERIC=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_OVERLAYFS=n
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
          CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_MANUAL_HOOK=n
          CONFIG_KSU_HAS_MANUAL_HOOK=n
          # GKI requires kprobes symbols to be present
          CONFIG_KPROBES=y
          CONFIG_HAVE_KPROBES=y
          CONFIG_KRETPROBES=y
          CONFIG_KPROBE_EVENTS=y
          EOF
          else
            cat >> $TARGET_CONFIG <<EOF
            CONFIG_KSU=y
            CONFIG_KPM=n
            CONFIG_KSU_THRONE_TRACKER_ALWAYS_THREADED=y
            CONFIG_KSU_LSM_SECURITY_HOOKS=y
            CONFIG_KSU_EXTRAS=y
            CONFIG_KSU_TAMPER_SYSCALL_TABLE_y
          EOF
          fi

      # --- SET VERSION NAME (Bersihin Spasi) ---
      - name: Setup Version Name
        working-directory: gki/common
        run: |
          # Ambil input, hapus semua spasi biar gak ngerusak Makefile
          RAW_NAME="${{ github.event.inputs.kernel_name }}"
          CLEAN_NAME=$(echo $RAW_NAME | tr -d '[:space:]')
          
          rm -f localversion*
          sed -i 's/^EXTRAVERSION =.*/EXTRAVERSION =/' Makefile
          echo "-$CLEAN_NAME" > localversion
          echo "✅ Version set to: -$CLEAN_NAME"

      - name: Setup Compiler (Clang 17)
        working-directory: gki
        run: |
          # Path absolut sesuai build.config.common lu: clang-r563880b
          CLANG_DIR="prebuilts-master/clang/host/linux-x86/clang-r563880b"
          mkdir -p "$CLANG_DIR"
          # Kita pake Clang 17 (Stable) untuk kestabilan Infinix/MTK
          curl -L "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/tags/android-14.0.0_r1/clang-r487747c.tar.gz" | tar -xz -C "$CLANG_DIR"

      # --- EXECUTE BUILD (Tambah Pembersihan Staging) ---
      - name: Execute Build
        working-directory: gki
        run: |
          # PENTING: Hapus folder staging sisa build sebelumnya (Pemicu Error 1)
          rm -rf out/android12-5.10/staging
          
          export KMI_SYMBOL_LIST_STRICT_MODE=0
          export KMI_ENFORCED=0
          export TRIM_NONLISTED_KMI=0
          
          # Gunakan PATH absolut untuk compiler
          export PATH=$GITHUB_WORKSPACE/gki/prebuilts-master/clang/host/linux-x86/clang-r563880b/bin:$PATH
          
          LTO=thin LLVM=1 LLVM_IAS=1 \
          KCFLAGS="-O3 -Wno-error=implicit-function-declaration -Wno-error=unused-command-line-argument" \
          BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh

      - name: Packaging anykernel & Release
        if: success()
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          FULL_VER=$(make -s kernelversion -C gki/common | tr -d '\n\r')
          DATE=$(date +"%Y%m%d")
          VARIANT="${{ matrix.ksu_variant }}"
          KNAME="${{ github.event.inputs.kernel_name }}"
          ZIP_NAME="AK3-500hz-${VARIANT}-${KNAME}-${DATE}.zip"
          
          git clone -q --depth=1 "https://github.com/zixine/anykernel" -b main anykernel
          # Berdasarkan MAKE_GOALS lu, cari Image.lz4
          IMG=$(find gki/out -name "Image.lz4" -type f | head -n 1)
          if [ -z "$IMG" ]; then IMG=$(find gki/out -name "Image" -type f | head -n 1); fi
          
          cp "$IMG" anykernel/
          sed -i "s|kernel.string=.*|kernel.string=${FULL_VER} by Zixine|g" anykernel/anykernel.sh
          
          cd anykernel && zip -r9 "../$ZIP_NAME" * -x .git .gitignore README.md && cd ..
          
          TARGET_REPO="zixine/Kernel-GKI-12-5.10-release"
          RELEASE_TAG="${KNAME}-${DATE}"
          
          if ! gh release create "$RELEASE_TAG" "$ZIP_NAME" --repo "$TARGET_REPO" --title "Zixine ${DATE}" --notes "Varian: ${VARIANT}" --prerelease=false 2>/dev/null; then
            gh release upload "$RELEASE_TAG" "$ZIP_NAME" --repo "$TARGET_REPO" --clobber
          fi

      - name: Archive Build Log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ matrix.ksu_variant }}
          path: gki/out/android12-5.10/common/build.log
