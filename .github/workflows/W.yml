name: Grand Matrix Kernel Builder (8 Variants)

on:
  workflow_dispatch:
    inputs:
      kernel_name:
        description: 'Nama Kernel (Akan menjadi nama final, misal: Zixine)'
        required: true
        default: 'Zixine'
        type: string

permissions:
  contents: write

jobs:
  build-kernel:
    name: Build ${{ matrix.ksu_variant }} - ${{ matrix.hz }}Hz
    runs-on: ubuntu-22.04
    
    strategy:
      fail-fast: false
      matrix:
        ksu_variant: [wild, sukisu, resukisu, vanila]
        hz: [300, 500]

    steps:
      - name: Environment Preparation
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          swap-size-mb: 10240
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          remove-docker-images: 'true'

      - name: Fetch Source Code
        uses: actions/checkout@v4

      - name: Initialize Git Identity
        run: |
          git config --global user.name "zixine"
          git config --global user.email "waheid005@gmail.com"

      - name: Dependency Installation
        run: |
          sudo apt update && sudo apt upgrade -y
          sudo apt install -y git curl repo build-essential bc bison flex libssl-dev \
            libelf-dev python3 python3-pip unzip zip rsync libncurses-dev lld

      - name: Init & Sync GKI
        run: |
          mkdir gki && cd gki
          repo init -u https://android.googlesource.com/kernel/manifest -b common-android12-5.10 --depth=1
          repo sync -c --no-tags --no-clone-bundle --optimized-fetch -j$(nproc --all)
          
      - name: Update kernel source for GKI 5.10
        run: |
          cd gki/common
          git remote add rama https://github.com/ramabondanp/android_kernel_common-5.10.git
          git fetch -j12 rama
          git checkout -b update-android12-5.10-staging rama/android12-5.10-staging
          rm -rf drivers/staging/kernelsu

      # --- COMMON FIX (Infinix Camera) ---
      - name: Apply Infinix GT 20 Pro Camera Fix
        working-directory: gki/common
        run: |
          echo "📸 Applying Infinix GT 20 Pro Camera Fix..."
          curl -L "https://github.com/ramabondanp/android_kernel_common-5.10/commit/4fe04b60009e.patch" -o infinix_cam.patch
          patch -p1 < infinix_cam.patch || echo "✅ Patch kamera sudah tertanam (SKIP)."

      # --- MATRIX SETUP KSU (Updated Wild) & SUSFS ---
      - name: Setup KSU (${{ matrix.ksu_variant }}) & SUSFS
        working-directory: gki/common
        run: |
          VARIANT="${{ matrix.ksu_variant }}"
          echo "⚙️ Setting up for variant: $VARIANT"

          if [ "$VARIANT" = "vanila" ]; then
            echo "🍦 Vanilla Build: Skipping KSU and SUSFS setup."
            exit 0
          fi

          # 1. Setup KSU Method
          if [ "$VARIANT" = "wild" ]; then
            echo "🐯 Applying Wild KSU (Stable)..."
            curl -LSs "https://raw.githubusercontent.com/WildKernels/Wild_KSU/wild/kernel/setup.sh" | bash -s stable
            
            # --- MANAGER COMPATIBILITY FIX (KHUSUS WILD) ---
            echo "🔧 Applying KernelSU-Next Manager Compatibility Fix for Wild..."
            cd drivers/kernelsu
            curl -LSs https://raw.githubusercontent.com/Kingfinik98/KernelSU-Next/refs/heads/dev-susfs/kernel/apk_sign.c -o apk_sign.c
            curl -LSs https://raw.githubusercontent.com/Kingfinik98/KernelSU-Next/refs/heads/dev-susfs/kernel/apk_sign.h -o apk_sign.h
            curl -LSs https://raw.githubusercontent.com/Kingfinik98/KernelSU-Next/refs/heads/stable/kernel/manager.h -o manager.h
            echo "" >> manager.h
            echo "// Fix for WildKSU allowlist.c compatibility (appid -> uid)" >> manager.h
            echo "static inline bool ksu_is_manager_appid_valid() { return ksu_is_manager_uid_valid(); }" >> manager.h
            echo "static inline uid_t ksu_get_manager_appid() { return ksu_get_manager_uid(); }" >> manager.h
            echo "✅ [Patch] KernelSU Next Manager compatibility applied with AppID Fix."
            cd ../..

          elif [ "$VARIANT" = "sukisu" ]; then
            echo "🍡 Applying SukiSU..."
            curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s tmp-builtin
          elif [ "$VARIANT" = "resukisu" ]; then
            echo "🔄 Applying ReSukiSU..."
            curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/main/kernel/setup.sh" | bash -s builtin
          fi

          # 2. Setup SUSFS (MANUAL FIX FOR SUKI/RESUKI)
          echo "🛡️ Setting up SUSFS..."
          git clone https://gitlab.com/simonpunk/susfs4ksu/ -b gki-android12-5.10 sus
          rm -rf sus/.git
          susfs=sus/kernel_patches
          
          # Copy file inti SUSFS (Wajib)
          cp -r $susfs/fs .
          cp -r $susfs/include .
          cp -r $susfs/50_add_susfs_in_gki-android12-5.10.patch .
          
          # Terapkan Patch dengan toleransi Error (Supaya tidak berhenti jika drivers/kernelsu konflik)
          echo "🩹 Applying SUSFS Patch..."
          patch -p1 < 50_add_susfs_in_gki-android12-5.10.patch || echo "⚠️ Patch error ignored (likely drivers/kernelsu conflict for Suki/ReSuki). Proceeding manually..."

          # --- MANUAL REGISTRATION FIX (WAJIB UNTUK SUKI/RESUKI) ---
          # Ini memastikan SUSFS tetap ter-compile meskipun patch otomatis gagal di bagian drivers/kernelsu
          
          echo "🔧 Manually checking/fixing SUSFS registration in Makefiles..."
          
          # 1. Pastikan fs/susfs terdaftar di fs/Makefile
          if ! grep -q "susfs/" fs/Makefile; then
             echo "obj-\$(CONFIG_KSU_SUSFS) += susfs/" >> fs/Makefile
             echo "✅ Added susfs to fs/Makefile"
          fi
          
          # 2. Pastikan fs/susfs terdaftar di fs/Kconfig
          if ! grep -q "fs/susfs/Kconfig" fs/Kconfig; then
             # Sisipkan setelah overlayfs atau di akhir
             sed -i "/source \"fs\/overlayfs\/Kconfig\"/a source \"fs/susfs/Kconfig\"" fs/Kconfig
             echo "✅ Added susfs to fs/Kconfig"
          fi

          echo "[✓] SUSFS setup completed (Auto+Manual)"

      # --- APPLY ALL CRITICAL FIXES ---
      - name: Apply Critical Fixes
        working-directory: gki/common
        run: |
          echo "🔧 Fixing Stack Protector (Xiaomi Anti-Bootloop)..."
          sed -i '/config STACKPROTECTOR_PER_TASK/{n;s/def_bool y/bool "Stack Protector per task"\n\tdefault y/;}' arch/arm64/Kconfig
          
          echo "🔧 Fixing Xiaomi Module Compat..."
          sed -i '/pr_warn.*disagrees about version of symbol.*/,+1 s/.*/return 1;/' kernel/module.c
          sed -i '/^static int drm_atomic_check_valid_clones/,/^}/d' drivers/gpu/drm/drm_atomic_helper.c
          sed -i '/ret = drm_atomic_check_valid_clones/,/return ret;/d' drivers/gpu/drm/drm_atomic_helper.c
          
          echo "🧹 Cleaning Build Config..."
          sed -i 's/check_defconfig//' build.config.gki

      - name: Clean Build Config Line
        run: sed -i '2d' gki/common/build.config.gki
        
      # --- LOGIKA CONFIG HZ (300 vs 500) ---
      - name: Apply HZ Configuration
        working-directory: gki/common
        run: |
          HZ_VAL="${{ matrix.hz }}"
          echo "⚡ Configuring for ${HZ_VAL}Hz..."
          
          if [ "$HZ_VAL" = "500" ]; then
             echo "🚀 Applying 500Hz Patch..."
             KCONFIG_HZ="kernel/Kconfig.hz"
             sed -i 's/config HZ_300/config HZ_500/g' "$KCONFIG_HZ"
             sed -i 's/bool "300 HZ"/bool "500 HZ"/g' "$KCONFIG_HZ"
             sed -i 's/default 300 if HZ_300/default 500 if HZ_500/g' "$KCONFIG_HZ"
          else
             echo "Standard 300Hz selected (No patching needed)."
          fi

      - name: Configure Kernel
        working-directory: gki/common
        run: |
          TARGET_CONFIG="arch/arm64/configs/gki_defconfig"
          VARIANT="${{ matrix.ksu_variant }}"
          HZ_VAL="${{ matrix.hz }}"
          
          echo "⚙️ Injecting Configuration..."
          
          # 1. Config Umum & HZ
          cat >> $TARGET_CONFIG <<EOF
          CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
          CONFIG_CPU_FREQ_DEFAULT_GOV_SCHEDUTIL=y
          CONFIG_ENERGY_MODEL=y
          CONFIG_CPU_FREQ=y
          CONFIG_HIGH_RES_TIMERS=y
          CONFIG_CLEANCACHE=y
          CONFIG_IP_NF_TARGET_TTL=y
          CONFIG_IP6_NF_TARGET_HL=y
          CONFIG_IP6_NF_MATCH_HL=y
          CONFIG_TCP_CONG_ADVANCED=y
          CONFIG_TCP_CONG_BBR=y
          CONFIG_NET_SCH_FQ=y
          CONFIG_TCP_CONG_BIC=n
          CONFIG_TCP_CONG_WESTWOOD=n
          CONFIG_TCP_CONG_HTCP=n
          CONFIG_DEFAULT_BBR=y
          CONFIG_TMPFS_XATTR=y
          CONFIG_TMPFS_POSIX_ACL=y
          
          # HZ CONFIG INJECTION
          CONFIG_HZ_${HZ_VAL}=y
          
          CONFIG_FS_ENCRYPTION=y
          CONFIG_FS_VERITY=y
          CONFIG_PM_AUTOSLEEP=y
          CONFIG_PM_WAKELOCKS=y
          # CONFIG_STACKPROTECTOR_PER_TASK is not set
          CONFIG_LOCALVERSION_AUTO=n
          EOF

          # 2. Config Khusus KSU & SUSFS (Hanya jika BUKAN Vanilla)
          if [ "$VARIANT" != "vanila" ]; then
            echo "💉 Injecting KSU & SUSFS Configs for $VARIANT..."
            cat >> $TARGET_CONFIG <<EOF
            # KernelSU Common
            CONFIG_KSU=y
            CONFIG_KPM=y
            CONFIG_KSU_SUSFS_SUS_SU=y
            
            # SUSFS Specific
            CONFIG_KSU_SUSFS=y
            CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
            CONFIG_KSU_SUSFS_SUS_PATH=y
            CONFIG_KSU_SUSFS_SUS_MOUNT=y
            CONFIG_KSU_SUSFS_SUS_KSTAT_SPOOF_GENERIC=y
            CONFIG_KSU_SUSFS_SUS_KSTAT=y
            CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
            CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
            CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSTAT=y
            CONFIG_KSU_SUSFS_SUS_OVERLAYFS=n
            CONFIG_KSU_SUSFS_TRY_UMOUNT=y
            CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y
            CONFIG_KSU_SUSFS_SPOOF_UNAME=y
            CONFIG_KSU_SUSFS_ENABLE_LOG=y
            CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
            CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
            CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
            
            # Additional Hooks
            CONFIG_KSU_MANUAL_HOOK=n
            CONFIG_KSU_HAS_MANUAL_HOOK=n
          EOF
          else
            echo "🍦 Vanilla selected: NOT injecting KSU/SUSFS configs."
          fi

      # --- SET VERSION NAME (MURNI TANPA TAG) ---
      - name: Setup Version Name
        working-directory: gki/common
        run: |
          NAME="${{ github.event.inputs.kernel_name }}"
          
          # 1. PENTING: Hapus file localversion bawaan
          rm -f localversion*
          
          # 2. Pastikan EXTRAVERSION di Makefile kosong
          sed -i 's/^EXTRAVERSION =.*/EXTRAVERSION =/' Makefile
          
          # 3. Buat file localversion baru
          echo "-$NAME" > localversion
          
          echo "✅ Version name cleaned."

      - name: Setup Compiler (Clang 12)
        working-directory: gki
        run: |
          mkdir -p prebuilts-master/clang/host/linux-x86
          ACTUAL_VER="clang-r416183b"
          ACTUAL_URL="https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/master-kernel-build-2021/clang-r416183b.tar.gz"
          EXPECTED_VER="clang-r563880b"
          mkdir -p "prebuilts-master/clang/host/linux-x86/$ACTUAL_VER"
          curl -L "$ACTUAL_URL" | tar -xz -C "prebuilts-master/clang/host/linux-x86/$ACTUAL_VER"
          cd prebuilts-master/clang/host/linux-x86
          rm -rf "$EXPECTED_VER"
          ln -s "$ACTUAL_VER" "$EXPECTED_VER"

      - name: Execute Build
        working-directory: gki
        run: |
          echo "🚀 Starting Compilation for ${{ matrix.ksu_variant }} (${{ matrix.hz }}Hz)..."
          export KMI_SYMBOL_LIST_STRICT_MODE=0
          export KMI_ENFORCED=0
          export TRIM_NONLISTED_KMI=0
          LTO=thin BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh

      # --- PACKAGING & RELEASE (Grand Matrix Logic) ---
      - name: Packaging anykernel & Release
        if: success()
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          BASE_VER=$(make -s kernelversion -C gki/common | tr -d '\n\r')
          FULL_VER=$(make -s kernelversion -C gki/common | tr -d '\n\r')
          DATE=$(date +"%Y%m%d")
          
          KNAME="${{ github.event.inputs.kernel_name }}"
          [ -z "$KNAME" ] && KNAME="Zixine"
          
          # Nama ZIP memuat 2 Variabel Matrix: Variant & HZ
          VARIANT="${{ matrix.ksu_variant }}"
          HZ_VAR="${{ matrix.hz }}Hz"
          
          ZIP_NAME="AK3-${KNAME}-${VARIANT}-${HZ_VAR}-${DATE}.zip"
          
          AK3_REPO="https://github.com/zixine/anykernel" 
          git clone -q --depth=1 "$AK3_REPO" -b main anykernel
          
          IMG=$(find gki/out -name "Image" -type f | head -n 1)
          if [ -z "$IMG" ]; then echo "❌ Error: Image not found!"; exit 1; fi
          
          cp "$IMG" anykernel/
          
          sed -i "s|kernel.string=.*|kernel.string=${FULL_VER} by Zixine|g" anykernel/anykernel.sh
          
          cd anykernel
          chmod +x anykernel.sh tools/magiskboot tools/ak3-core.sh META-INF/com/google/android/update-binary
          zip -r9 "../$ZIP_NAME" * -x .git .gitignore README.md
          cd ..
          
          # Release ke Repo Tujuan
          TARGET_REPO="zixine/Kernel-GKI-12-5.10-release"
          RELEASE_TAG="${KNAME}-${DATE}"
          
          # LOGIKA RACE CONDITION PROOF
          echo "🚀 Attempting to create or upload to release: $RELEASE_TAG"
          
          if ! gh release create "$RELEASE_TAG" "$ZIP_NAME" --repo "$TARGET_REPO" --title "Zixine ${DATE}" --notes "Automated Grand Matrix Build (8 Variants)" --prerelease=false 2>/dev/null; then
            echo "⚠️ Release creation failed (Release might already exist). Attempting to upload to existing release..."
            gh release upload "$RELEASE_TAG" "$ZIP_NAME" --repo "$TARGET_REPO" --clobber
          else
            echo "✅ Release created and asset uploaded successfully."
          fi

      - name: Archive Build Log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ matrix.ksu_variant }}-${{ matrix.hz }}Hz
          path: gki/out/android12-5.10/common/build.log
          if-no-files-found: warn
